{% extends 'index.html.twig' %}

{% block title %}Dish{% endblock %}

{% block body %}
    <h1>Dish</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish.id }}</td>
            </tr>
            <tr>
                <th>Dish_name</th>
                <td>{{ dish.dishName }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row  align-items-center">
        <div class="col-2">
            <a class="btn btn-info btn-sm btnStandard" max-width="150" href="{{ path('dish_index') }}">
                <i class="fa-solid fa-rotate-left fa-xs mx-1"></i>
                <p class="mobile_show"> </p>
                <span class="desktop_show">Retour à la liste</span>
            </a>
        </div>
        <div class="col-2">
            <a class="btn btn-info btn-sm btnStandard" href="{{ path('dish_edit', {'id': dish.id}) }}">
                <i class="fa-solid fa-pen fa-xs mx-1"></i>
                <p class="mobile_show"> </p>
                <span class="desktop_show">Modifier</span>
            </a>
        </div>
        <div class="col-2">
            {{ include('dish/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}
