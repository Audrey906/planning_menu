<form method="post" action="{{ path('unity_ingredient_delete', {'id': unity_ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unity_ingredient.id) }}">
    <button class="btn">Delete</button>
</form>
