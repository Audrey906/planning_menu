<!DOCTYPE html>

<title>PreparationTime</title>

{% block body %}
    <h1>PreparationTime</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preparation_time.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ preparation_time.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('preparation_time_index') }}">back to list</a>

    <a href="{{ path('preparation_time_edit', {'id': preparation_time.id}) }}">edit</a>

    {{ include('preparation_time/_delete_form.html.twig') }}
{% endblock %}
