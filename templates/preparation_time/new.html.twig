<!DOCTYPE html>

<title>New PreparationTime</title>

{% block body %}
    <h1>Create new PreparationTime</h1>

    {{ include('preparation_time/_form.html.twig') }}

    <a href="{{ path('preparation_time_index') }}">back to list</a>
{% endblock %}
