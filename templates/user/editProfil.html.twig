{% extends 'index.html.twig' %}

{% block title %}Modifier votre profil{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12 offset-0 offset-md-3 col-md-6">
                 <h1 class="my-4">Modifier votre profil</h1>
                {{ form_start(formProfil) }}
                    {{ form_row(formProfil.pseudo) }}
                    {{ form_row(formProfil.email) }}
                    <button class="btn btn-dark text-info">{{ button_label|default('Enregistrer') }}</button>
                {{ form_end(formProfil) }}
                <a class="btn  btn-dark text-info mt-4" href="{{ path('profil') }}">Retour</i></a>
            </div>
        </div>
       
    </div>
</div>

    
{% endblock %}
