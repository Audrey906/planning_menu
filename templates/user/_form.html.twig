 {{ form_start(form) }}
    {{ form_row(form.pseudo) }}
    {{ form_row(form.email) }}
    {# {{ form_row(form.roles) }} #}
    <button class="btn btn-info">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
